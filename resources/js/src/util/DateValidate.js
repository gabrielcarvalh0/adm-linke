export default class DateValidate {

    static dateToMinutes(small) {
        var date = new Date
        var seconds = date.getSeconds()
        
        return console.log(small.innerHTML = seconds)
        // return  `agora há ${seconds}`
    }
}